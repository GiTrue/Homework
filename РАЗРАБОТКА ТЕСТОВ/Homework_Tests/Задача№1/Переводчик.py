Задание «Переводчик»
Условие задачи
У Яндекса есть очень удобное API для словарей. Там можно переводить слова с одного языка на другой.

Ваша задача – написать функцию, которая принимает русское слово и возвращает его перевод на английском языке. Для работы с API вам потребуется получить токен.

В описании введите «Для обучения» и вы получите бесплатный токен.

Если с этим будут трудности вы можете воспользоваться токеном из подсказки.



import requests

# Базовый URL для обращения к API Яндекс.Словаря
url = 'https://dictionary.yandex.net/api/v1/dicservice.json/lookup'
# Токен для доступа к API. Его получают в личном кабинете Яндекс.Разработчика.
token = 'YANDEX_API_KEY'

def translate_word(word):
    params = {
        'key': token,       # ключ для авторизации в API
        'lang': 'ru-en',    # направление перевода: с русского на английский
        'text': word        # слово, которое нужно перевести
    }

    # Выполняем GET-запрос к API со всеми параметрами
    response = requests.get(url, params=params)

    # Преобразуем JSON-ответ в Python-словарь
    data = response.json()

    # Проверяем, содержит ли ответ переводы
    if data.get('def'):
        # Возвращаем текст первого перевода из списка
        return data['def'][0]['tr'][0]['text']


if __name__ == '__main__':
    word = 'машина'
    assert translate_word(word) == 'car'
